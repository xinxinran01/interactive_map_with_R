# 0.1.0 验证记录

本机 Windows、R 4.5.2：合成三地区示例实际运行。
连续地图、固定断点地图、300 DPI PNG 均生成；记录数 3，合计 68。
不覆盖已有输出；不覆盖已有示例目录；断点不覆盖数据范围时停止。
未执行跨平台测试；点图层和 CSV 冲突分支还需更多真实案例测试。
浏览器交互自动化未执行，安装者可打开示例手动检查。

当前 Windows 会话预设 LC_ALL=C.UTF-8 导致 R 中文路径异常。
测试进程设置 LC_ALL=English_United States.utf8、LANG=en_US.UTF-8 后通过。
这是进程设置，不应擅自修改用户全局区域配置。
